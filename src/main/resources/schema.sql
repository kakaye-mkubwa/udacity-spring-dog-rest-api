CREATE TABLE dog(
                    id INT AUTO_INCREMENT PRIMARY KEY,
                    dogName VARCHAR (200),
                    breed VARCHAR  (200),
                    origin VARCHAR (200)
);